# 법령안 출력 포맷 스펙 (Formatting)

> **주의**: 이 스펙은 법제처 「법령 입안·심사 기준」·「법제업무편람」(chNN) 요약이며, 최종 형식은 원문과 공식 법령안편집기(정부입법지원센터)로 확인한다. 여기 규칙에 없는 세부 서식은 추측·창작하지 말고 원문을 확인한다.

에이전트가 초안 조문·개정문을 생성할 때 그대로 적용하는 규칙서. 출처 `(chNN)`.

---

## 1. 번호체계 — 조·항·호·목·세목

| 단위 | 마커 | 내용 성격 | 예 |
|---|---|---|---|
| 조(條) | `제1조`·`제2조` | 규율 단위 | `제5조(허가) …` |
| 항(項) | `①`·`②`·`③` (원문자) | 완성된 문장 | `① …한다.` |
| 호(號) | `1.`·`2.`·`3.` | 단어·어절 또는 "…할 것" (원칙적으로 "…한다"는 안 씀) | `1. …할 것` |
| 목(目) | `가.`·`나.`·`다.` | 단어·어절 | `가. …` |
| 세목 이하 | `1)` → `가)` → `(1)` → `(가)` 순 | 목 아래 더 세분 | `1)`, `가)`, `(1)`, `(가)` |

- 항은 완성된 문장으로 `①②③…` 원문자로 표시한다 (ch31, ch33).
- 호는 `1. 2. 3.`, 단어·어절 또는 "…할 것" 형식이며 원칙적으로 "…한다"를 쓰지 않는다. 예외: 호에 단서·후단을 두거나 정의 규정("~란 ~을 말한다")일 때 (ch31, ch33).
- 목은 `가. 나. 다.`, 목 이하 세분은 `1) → 가) → (1) → (가)` 순 (ch31).
- **조 제목**: 조 번호 바로 뒤 괄호로 붙인다 — `제5조(허가)`. 괄호를 포함한 제목 전체가 개정 단위다 (ch31, ch33).
- 항이 하나뿐인 조는 항 번호(`①`)를 표시하지 않고 본문을 바로 쓴다 (ch31).
- 항이 3개 이상 문장이면 전단·후단·단서 특정이 어려우므로 두 문장 이하로 작성 (ch31).

### 들여쓰기·간격
- 개정문 인용에서 조·항·호·목은 **붙여** 쓴다: `제5조제1항제2호` (ch32).
- 본문·단서·전단·후단은 **띄어** 쓰고, `중`도 띄어 쓴다: `제5조제1항 본문 중` (ch32).
- ※ 화면상 들여쓰기 폭·글머리 위치의 물리 규격은 편집기 기본값을 따른다(원문 확인 필요). 이 스펙은 마커·간격 규칙만 결정론적으로 규정한다.

---

## 2. 조문 인용 방식

- **해당 법령 내부 인용**: "이 법/이 영/이 규칙"을 생략하고 `제○조제○항` 만 쓴다 (ch31).
- **다른 법령 인용**: 제명 + 조항. 재차 인용 시 `같은 법/같은 영/같은 규칙 제○조` (ch31).
- 본칙에서 법령명 인용 시 앞뒤에 낫표 `「 」` 를 쓴다 (2005. 1. 1. 이후, 띄어쓰기 함께) (ch32).
- **부칙에서 본칙 인용**은 조문만, 부칙 내 인용은 `부칙 제○조` (ch31, ch27).
- 개정된 본칙 조문을 부칙에서 인용할 때는 전부·일부개정 불문 `제○조의 개정규정` 으로 표현("신설/삭제 규정" 금지) (ch27).
- **각 호 일부 인용**: 전부="다음 각 호의 요건에 모두 해당", 하나="제○호…의 어느 하나에 해당" / "제○호부터 제○호까지의 어느 하나에 해당". 다른 호가 필수·배제 요건이면 "어느 하나" 사용 금지 (ch31).

### 가지번호(제○조의2)
- 뒤 조·호 번호를 밀지 않으려고 신설 조·호에 붙인다: `제5조의2` (ch31, ch33).
- **조·호와 하위법령 별표·서식의 목에만** 쓰며, **항에는 가지번호를 쓰지 않는다** (항 사이 신설은 현행 항 이동 방식) (ch33).
- 가지번호가 있으면 삭제·준용·인용 시 반드시 명시한다("제2조부터 제5조까지, 제5조의2 …") (ch33).

---

## 3. 개정지시문·개정문

**용어 구분** (ch32):
- **개정지시문** = 법령 전체 개정 선언: `○○법 일부를 다음과 같이 개정한다.` / 전부개정: `○○법 전부를 다음과 같이 개정한다.`
- **개정문** = 조문별 지시: `제○조제○항 중 「A」를 「B」로 한다.`

**작성 원칙** (ch32):
- 조 단위·조문 순서로 작성. 한 개정문에 여러 사항은 `~하고, ~하며, …로 한다`로 연결(한 문장 내 둘 이상이면 `~로, ~로`). 너무 길면 이해하기 쉽게 끊는다.
- 개정 자구는 관련 있는 **최소 명사(복합명사) 단위**로만 인용. 조사·수식어는 넣지 않는다 (○ `'국토해양부장관'을 '국토교통부장관'으로` / × `'국토해양장관은'을 …`).
- 같은 조 안 같은 사항을 개정하면 `각각` 을 표시: `제○조제○항 및 제○항 중 「─」을 각각 「─」로 한다`.

### 자구 일부개정 (ch33)
- 하나: `제○조 중 「∼」을 「∼」로 한다.`
- 같은 조·항·호 둘 이상: `제○조 중 「∼」을 「∼」로, 「∼」을 「∼」로 한다.`
- 항·호가 다르면: `제○조제○항 중 …로 하고, 같은 조 제○항 중 …로 한다.`
- 각 호 외의 부분: `제○조 각 호 외의 부분 (본문) 중 …`.

### 신설 (ch32, ch33)
- 표현은 `신설한다`("삽입·추가" 금지). 자구 추가도 대체 방식으로: `「서울특별시」를 「서울특별시·부산광역시」로 한다`.
- 맨 끝: `제6조를 다음과 같이 신설한다.` / `제○조에 제6항을 다음과 같이 신설한다.` / `제○조제○항에 제6호를 다음과 같이 신설한다.`
- 항 없는 조에 제2항 이하 신설: `제○조 제목 외의 부분을 제1항으로 하고, 같은 조에 제2항을 다음과 같이 신설한다.`
- 사이 신설(현행 조항 이동): `제○조제3항을 제5항으로 하고, 같은 조에 제3항 및 제4항을 각각 다음과 같이 신설한다.` — 뒤 조문 밀림·인용이 많으면 가지번호 방식 선택. 관계 법령 인용도 함께 정비.

### 전부개정(조·항·호 단위) (ch33)
- 조: `제○조를 다음과 같이 한다.` / 항·호: `제○조제2항을 다음과 같이 한다.` / 다수: `제○조제2항부터 제5항까지를 각각 다음과 같이 한다.`

### 삭제 (ch32, ch33)
- `제○조(항·호)를 삭제한다.` ("폐지한다" 금지). 다수: `제○조부터 제○조까지를 각각 삭제한다.` 중간에 이미 삭제된 조·가지번호가 있으면 반드시 명시.

### 전부개정 법령안 형식 (ch35)
- 제명에 이어 `전부개정법률안`·`전부개정령안` 표기.
- 순서: 개정지시문(`○○법 전부를 다음과 같이 개정한다.`) → 줄 바꿔 개정 후 제명 → 본칙·부칙을 제정처럼 차례로 기재.
- 전부개정 시 특별한 규정이 없으면 종전 부칙은 실효 (ch32).

### 장·절·부칙 개정 (ch34)
- 장·절 제목: `제○장(절)의 제목 중 「─」을 「─」로 한다`. 추가·삭제 시 `(제○조부터 제○조까지)` 범위 병기.
- 조문 변동 없이 장·절 제목만 추가: `제○조 다음에 장 번호 및 제목을 다음과 같이 신설한다`.
- 기존 부칙 개정은 **본칙에서** 개정문을 붙여서 하고, 공포번호+제명을 병기: `법률 제○호 ○○○법 일부개정법률 부칙 제○조제○항을 다음과 같이 한다`.

### 관련 개정·폐지 (ch36)
- 부칙에서 다른 법령 개정: 조 제목 `(다른 법령의 개정)`, 다수면 법령별 항 분리.
- 본칙 일괄개정: 대상 법령별 조 + 조 제목 `(○○법의 개정)`.
- 폐지: 부칙 `제○조(다른 법률의 폐지) 법률 제○○○호 ○○법을 폐지한다.` 또는 별도 `○○법 폐지법률안`.

---

## 4. 별표·서식 표기 (ch31, ch34)

- **제목**에는 대괄호를 붙인다: `[별표 1]`, `[별지 제1호서식]`. 별표 제목에 밑줄, 괄호 안에 관련 조항: `[별표 1] ○○ 시험과목(제○조 관련)`.
- **법령문 안에서 인용**할 때는 대괄호 없이 `별표`·`별지 서식`.
- 별표 개정 표기는 대괄호·제목 없이: 전부개정 `별표 2를 별지와 같이 한다`, 일부개정 `별표 1 중 대구청란을 다음과 같이 한다` (ch34).
- 서식 개정: `별지 제2호서식을 별지와 같이 한다`(괄호 없이). 서식 가지번호는 내용이 다르면 `제3호의2서식`, 같으면 `제3호서식(2)` (ch34).
- 서식은 총리령·부령에서 정함이 원칙 (ch31).

---

## 5. 부칙 서식 (ch27, ch31)

- 맨 앞에 반드시 `부칙` 표시(본칙에는 표시하지 않음). 장 번호를 붙이지 않고 조로 구분, 조 번호는 제1조부터 새로 시작. 시행일만 규정하면 조·제목 미표시 (ch27, ch31).
- **배열 순서**: ① 시행일 → ② 유효기간 → ③ 기존 법령 폐지 → ④ 시행 준비행위 → ⑤ 적용례 → ⑥ 특례 → ⑦ 경과조치 → ⑧ 다른 법령 개정 → ⑨ 다른 법령과의 관계 (ch27).
- **시행일** 네 방식: ① `이 법은 공포한 날부터 시행한다.` ② `이 법은 공포 후 ○개월이 경과한 날부터 시행한다.` ③ 특정한 날 ④ 특정 사실 연계. 하위법령 필요 법률은 공포 후 6개월 이상 유예. 위임은 "6개월이 넘지 아니하는 범위에서 대통령령으로 정하는 날" 식으로 범위를 정함 (ch27).
- **경과조치**: 개정 본칙 인용은 `제○조의 개정규정`, 부칙 내 인용은 `부칙 제○조` (ch27).
- 한시법·한시규정은 `적용기간`이 아니라 `유효기간` 으로 통일 (ch27).

---

## 6. 편집규격 (심사안, ch56)

법령심사안(국무회의 상정안)을 법령안편집기로 낼 때의 물리 규격. 초안 텍스트 자체가 아니라 최종 편집 단계 기준이므로 참고용.

- 용지 A4(210×297㎜), 여백 위·아래 16㎜/좌·우 25㎜/머리말·꼬리말 12.7㎜.
- **줄 간격**: 기본 230%, 신·구조문대비표 180%, 각 제목 위·법령 제명·부칙 위아래·편장절 제목 위는 460%.
- 글씨체 신명조, 장평 100%·자간 0%. 크기: 본문·법령안 내용 14pt, 부칙 제목 14pt(진한), 법령 제명·대비표 제목 16pt(진한).
- 정렬: 법령 제명·편장절 제목·부칙 제목·대비표 제목은 가운데, 그 밖은 양쪽 정렬.
- 주요내용 조문 표기: 제정·전부개정은 `(안 제○조)`, 일부개정은 조문 뒤 `신설`·`삭제` 표기.
- ※ 편집기 화면·메뉴는 개정될 수 있으므로 UI 세부 절차는 원문(법제업무편람 p.194–297) 확인 (ch56).

---

## 결정론적 포매터

번호체계·마커·개정문 조립은 `tools/format_lawtext.py`(표준 라이브러리만)로 결정론적으로 처리한다. 조 제목·항·호·목 마커 부여와 개정지시문 헬퍼를 제공한다. LLM 없이 구조화 입력(dict/JSON)을 위 마커로 포맷한다.
